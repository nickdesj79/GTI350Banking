<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nick.gti350banking">

    <application
        android:allowBackup="true"
        android:icon="@drawable/bankingonlineappicon"
        android:label="Banking Online"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConnexionScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".RightMenuActivity" />
        <activity android:name=".TransferActivity" />
        <activity android:name=".SchedualAppointmentActivity" />
        <activity android:name=".InteractTransferActivity"></activity>
    </application>

</manifest>